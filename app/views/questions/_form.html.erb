<%= form_with model: question do |form| %>
  <p><%= form.label :body, "Text" %></p>
  <p><%= form.text_area :body, rows: 5, class: "form_textarea" %></p>
  <p><%= form.label :body, "Text" %></p>
  <p><%= form.text_field :user_id, class: "form_textfield"%></p>
  <p><%= form.submit "Save", class: "button button_main"%></p>
<% end %>

